dependencies:
  override:
    - mvn clean install

deployment:
  aws:
    branch: serverless-dot-com
    commands:
      - npm install
      - cd services/myService
      - npm run serverless -- deploy